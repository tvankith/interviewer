# Important Rules

- Maximum 200 lines per file
- If file exceeds limit split into next page
- Never write Alembic migration
- Developer handles DB migration
- Reuse existing services/components first
- Follow current folder conventions
- Keep naming consistent